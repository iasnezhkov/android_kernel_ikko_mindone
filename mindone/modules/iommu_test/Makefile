# ../include/: MediaTek platform headers mirrored at their kernel-tree paths (shared by all modules)
obj-m += iommu_test.o
ccflags-y += -Wno-error -I$(src)  -I$(src)/../include/drivers/dma-buf/heaps 
